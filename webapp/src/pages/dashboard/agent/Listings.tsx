import { Building2 } from 'lucide-react';
import { PlaceholderPage } from '@/components/dashboard/PlaceholderPage';

export default function AgentListings() {
  return (
    <PlaceholderPage
      title="My Listings"
      description="Manage your property listings — add new properties, update details, upload photos, and track listing performance."
      Icon={Building2}
    />
  );
}
